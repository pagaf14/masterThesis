# tuples: cannot change

import os
os.system("clear")

numbers = (1, 2, 3)
# numbers. --> no adding, removing, etc tools
print(numbers.index(2))

# numbers[0] = 20 # error because we can't modify a tuple
